{
  "_comment": "Add the 'ghl' block under mcpServers in your ~/.claude/settings.json",
  "_instructions": [
    "1. Find your GHL Location ID: GHL → Settings → Company → Locations",
    "2. Replace REPLACE_WITH_YOUR_LOCATION_ID below",
    "3. Copy the 'ghl' block into ~/.claude/settings.json under mcpServers",
    "4. Run: ./mcp/setup.sh to clone + build the MCP server first"
  ],
  "mcpServers": {
    "ghl": {
      "command": "node",
      "args": ["~/.claude/mcp/ghl-mcp/dist/index.js"],
      "env": {
        "GHL_API_KEY": "pit-335bf0ee-b8e4-4eaa-be07-997052ceb717",
        "GHL_LOCATION_ID": "REPLACE_WITH_YOUR_LOCATION_ID",
        "GHL_BASE_URL": "https://services.leadconnectorhq.com",
        "NODE_ENV": "production"
      }
    }
  }
}
