# Output files — audit/audit_data.json kept in repo for Claude
audit/audit_data_*.json
/audit_data.json
# Builder-specific branding (optional — commit if you want it in repo)
audit/ghl_branding_captured.json

# Environment and secrets — NEVER commit these
.env
.env.local
.env.production
*.env
mcp/.env
secrets.json
credentials.json

# Node / MCP server build artifacts (installed locally, not committed)
node_modules/
dist/
build/
*.js.map

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/settings.json
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Python cache
__pycache__/
*.pyc
*.pyo
